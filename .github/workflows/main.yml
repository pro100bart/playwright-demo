name: Run Tests on PR

on:
  pull_request:
    branches:
      - main  

permissions:
  contents: write  
  actions: write  
  pull-requests: write  # Виправлення: Дозволяє публікувати коментарі в PR

jobs:
  test:
    runs-on: ubuntu-latest  
    steps:
      - name: Checkout репозиторію
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Install Playwright
        run: npx playwright install --with-deps

      - name: Run tests in headless-mode
        run: npx playwright test

      - name: Debug test results
        if: always()
        run: |
          echo "🔍 Перевірка директорії playwright-report"
          ls -la playwright-report || echo "WARNING playwright-report не знайдено!"

      - name: Save HTML Report (артефакт)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: html-report-${{ github.event.pull_request.number }}
          path: playwright-report/
          retention-days: 7  # Зберігати звіти 7 днів

  comment:
    needs: test  
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Checkout репозиторію
        uses: actions/checkout@v4

      - name: Download HTML Report
        uses: actions/download-artifact@v4
        with:
          name: html-report-${{ github.event.pull_request.number }}
          path: playwright-report/

      - name: Count Passed/Failed Tests
        run: |
          PASSED=$(grep -o "✓" playwright-report/index.html | wc -l || echo "0")
          FAILED=$(grep -o "✗" playwright-report/index.html | wc -l || echo "0")
          echo "PASSED_TESTS=$PASSED" >> $GITHUB_ENV
          echo "FAILED_TESTS=$FAILED" >> $GITHUB_ENV

      - name: Publish comment with test summary
        uses: mshick/add-pr-comment@v2
        with:
          message: |
            📊 **Результати тестування**
            ✅ Виконано тестів: `${{ env.PASSED_TESTS }}`
            ❌ Завалено тестів: `${{ env.FAILED_TESTS }}`
            🔗 **[Переглянути HTML-звіт](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts/${{ github.event.pull_request.number }})**
          repo-token: ${{ secrets.PAT_TOKEN }}
