name: Run Tests on PR

on:
  pull_request:
    branches:
      - main  

permissions:
  contents: write  
  actions: write  
  pull-requests: write  

jobs:
  test:
    runs-on: ubuntu-latest  
    steps:
      - name: Checkout репозиторію
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Install Playwright
        run: npx playwright install --with-deps

      - name: Debug Playwright Configuration
        run: |
          echo "Поточний Playwright config:"
          cat playwright.config.ts || echo "⚠️ WARNING: Файл playwright.config.ts не знайдено!"

      - name: Check Playwright Reporters
        run: npx playwright show-report --reporter=list || echo "WARNING Не вдалося отримати список репортерів!"

      - name: Run tests in headless-mode
        run: npx playwright test --reporter=html,json

      - name: Debug Playwright Report Directory
        if: always()
        run: |
          echo "🔍 Перевіряємо, чи існує JSON-звіт..."
          ls -la playwright-report || echo "⚠️ WARNING: Папка playwright-report не знайдена!"
          if [ -f "playwright-report/test-results.json" ]; then
            echo "✅ JSON-звіт знайдено!"
            cat playwright-report/test-results.json | head -n 20  # Вивести перші 20 рядків JSON для перевірки
          else
            echo "❌ JSON-звіт НЕ створено!"
          fi

      - name: Save HTML Report (артефакт)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: html-report-${{ github.event.pull_request.number }}
          path: playwright-report/
          retention-days: 7  
